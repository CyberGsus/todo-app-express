{
  "name": "note-app-vue",
  "description": "A simple note app with Vuetify frontend and Express backend",
  "license": "MIT",
  "scripts": {
    "setup:server": "cd server && yarn install",
    "setup:client": "cd client && yarn install",
    "setup:all": "yarn install:server && yarn install:client",
    "start:server": "cd server && yarn start",
    "build:client": "mkdir -p dist/backend server/public && cd client && yarn build"
  }
}
